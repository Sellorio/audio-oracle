@using System.Net
@inject NavigationManager NavigationManager

Redirect to login

@code {
	protected override void OnInitialized()
	{
		NavigationManager.NavigateTo("/login?redirectUrl=" + WebUtility.UrlEncode(NavigationManager.Uri));
	}
}
